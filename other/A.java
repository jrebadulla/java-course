package other;

public class A {
    public int marks = 80; // accessible from anywhere
    // protected int marks = 80; cant use outside the package, can use in different
    // package but sub class
    // private int marks = 80; // can use in the same class
    // int marks = 80; // not accessible, accessible in the same package
}
